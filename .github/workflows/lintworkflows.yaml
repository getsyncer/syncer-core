# THIS FILE IS AUTOGENERATED BY SYNCER  DO NOT EDIT
name: Lint Workflows
description: Lint workflow files using reviewdog

on:
  pull_request:
    branches:
      - main

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: reviewdog/action-actionlint@v1
        with:
          fail_on_error: true